# Demo copy — gateway multi-modelo (OpenRouter :free)

## Tweet / post curto
Gateway multi-modelo sem lock-in: uma API, dezenas de `:free`, fallback e Activity. Smoke + cotas oficiais em BRL via PIX.

## Pitch 30s
Se você testa LLMs e cansa de trocar SDK a cada provider, OpenRouter unifica o roteamento. No free-tier (`:free`) dá pra validar ideias com custo 0 — com cotas claras (RPM/RPD). Entrego smoke report + script mínimo.

## CTA
R$20 smoke report · R$50 smoke+setup. PIX recebedor LUIZ. USD via Binance (em breve).
